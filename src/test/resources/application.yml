server:
  port: 5000

spring:
  mail:
    host:
    port:
    username: bhuman.soni@gmail.com
    password:
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  security:
    jwt:
      secret:
    user:
      name:
      password:


aws:
  upload-limit: 5
  region:
  key: "dummy"
  secret: "dummy"
  dynamo-db:
    amazonDBEndpoint: http://localhost:8000
  s3UploadBucketName: